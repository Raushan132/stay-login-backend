{
  "name": "stay-signin",
  "version": "1.0.0",
  "description": "creating web application with both side validation and bcrpt password encoder to store secure passsword and jwt token to identify user is already login or not",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "mongoose": "^8.0.2"
  }
}
